### [Performance] 리플로우 Reflow

- HTML 요소들의 위치, 크기가 변경되면 브라우저가 레이아웃을 다시 계산하는 현상

```js
// 요소의 너비를 변경
element.style.width = "500px"; // 리플로우 발생

// 브라우저 💭
// 요소 너비가 바뀌었다 -> 리플로우 예약 -> 나중에 계산 -> 리플로우 실행
```

- 리플로우를 일으키는 주요 동작

```js
// 1. 스타일 변경
element.style.width = "500px";
element.style.height = "300px";
element.style.margin = "20px";
element.style.padding = "10px";
element.style.border = "1px solid red";
element.style.fontSize = "20px";

// 2. DOM 조작
element.appendChild(newElement); // 요소 추가
element.removeChild(oldElement); // 요소 제거
element.innerHTML = "<div>...</div>"; // HTML 변경

// 3. 클래스 변경
element.classList.add("active"); // 클래스 추가
element.className = "new-class"; // 클래스 교체

// 4. 레이아웃 정보 읽기 - 읽기만 해도 리플로우 ...
element.offsetWidth;
element.offsetHeight;
element.clientWidth;
element.clientHeight;
element.scrollTop;
element.getBoundingClientRect();
window.getComputedStyle(element);
```

- 리플로우는 매우 비싼 연산이므로 가능한 횟수를 감소시키는 것이 좋다.

```js
// as-is
element.style.width = 100 + i + "px"; // 리플로우 1
element.style.height = 100 + i + "px"; // 리플로우 2

// to-be
element.style.cssText = "width: 110px; height: 110px;";
```

## <br />

### [Performance] 리플로우 Reflow & 리페인트 Repaint

- 리플로우 : 레이아웃 재계산

  - 위치, 크기 등 레이아웃 변경
  - 비용이 비싼 편

- 리페인트 : 화면(픽셀) 다시 그리기

  - 색상 변경 등 리페인트만 일어날 경우 비용이 저렴한 편
  - 보통은 리플로우 발생시 리페인트도 수행된다

```js
// Repaint만 (빠름)
element.style.color = "red";
element.style.backgroundColor = "blue";

// Reflow + Repaint (느림)
element.style.width = "500px";
element.style.display = "none";
```
